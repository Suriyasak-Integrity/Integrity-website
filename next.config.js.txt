// next.config.js
module.exports = {
  webpack(config, options) {
    config.module.rules.push({
      test: /\.js$/,
      loader: 'babel-loader',
      options: {
        presets: ['next/babel'],
        sourceType: 'module',  // ตั้งค่า sourceType เป็น module
      },
    });
    return config;
  },
};
