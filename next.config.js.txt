// next.config.js

module.exports = {
  webpack(config) {
    // กำหนดให้ webpack รองรับ ES Modules
    config.module.rules.push({
      test: /\.js$/,
      loader: 'babel-loader',
      options: {
        presets: ['next/babel'],
      },
    });
    return config;
  },
};
